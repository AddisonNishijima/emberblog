<li>
{{post.day}} - {{post.topic}} - {{post.sciname}} - {{post.description}}
  {{#if isImageShowing }}
      <p><img src={{post.image}} alt={{post.description}} {{action 'imageHide'}}></p>
    {{else}}
      <button {{action 'imageShow'}}>Show image</button>
    {{/if}}
    <button {{action 'delete' post}}>Delete Post</button>
</li>
